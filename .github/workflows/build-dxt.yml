name: Build and Release DXT

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Pack DXT
        run: npx --yes @anthropic-ai/dxt@latest pack

      - name: Compute version and rename artifact
        id: compute
        shell: bash
        run: |
          set -euo pipefail
          VERSION=$(node -p "require('./package.json').version")
          FILE=$(ls -1 *.dxt | head -n1)
          NEW_NAME="fastmail-mcp-$VERSION.dxt"
          mv "$FILE" "$NEW_NAME"
          echo "dxt_file=$NEW_NAME" >> "$GITHUB_OUTPUT"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: fastmail-mcp-dxt
          path: ${{ steps.compute.outputs.dxt_file }}

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ steps.compute.outputs.dxt_file }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

